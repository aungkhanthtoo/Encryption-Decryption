class Circle {

    double radius;

    // write methods here
    double getArea() {
        return Math.PI * radius * radius;
    }

    double getLength() {
        return radius * 2 * Math.PI;
    }
}